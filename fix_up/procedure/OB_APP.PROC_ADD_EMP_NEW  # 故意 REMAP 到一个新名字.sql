-- 修补缺失的 PROCEDURE OB_APP.PROC_ADD_EMP_NEW  # 故意 REMAP 到一个新名字 (源: ORA_APP.PROC_ADD_EMP)
-- 本文件由校验工具自动生成，请在 OceanBase 执行前仔细审核。

CREATE OR REPLACE EDITIONABLE PROCEDURE "OB_APP"."PROC_ADD_EMP_NEW  # 故意 REMAP 到一个新名字" (
    p_name IN VARCHAR2
) AS
BEGIN
    NULL; -- ������������������������
END;