# 版本对比清单 — 0.9.8.1 → 0.9.8.2

对比范围：V0.9.8.1 与 V0.9.8.2

## 代码变更摘要
- PUBLIC/`__public` 语义归一化：OB 元数据归一后统一以 PUBLIC 比对与输出。
- FK 约束比对补齐 `UPDATE_RULE`。
- obclient SQL 改 stdin 传入，避免特殊字符失败。
- 致命错误改为异常链路，主流程统一捕获退出。

## 文档变更
- 版本号更新至 0.9.8.2
- 新增 `RELEASE_NOTES_0.9.8.2.md`
- 变更日志与技术规格同步更新

## 兼容性
- 对已有配置无破坏性变更。
- 触发器逻辑未改动。

## 说明
- PUBLIC 语义统一仅影响报告展示与逻辑归一，不影响 OB 实际 owner 名称。
