## 1. Implementation
- [ ] 1.1 Normalize `DOUBLE PRECISION` to `BINARY_DOUBLE` in DDL cleanup for fixup generation.
- [ ] 1.2 Normalize column type comparisons to treat `DOUBLE PRECISION` as `BINARY_DOUBLE` if encountered in metadata.
- [ ] 1.3 Add unit tests for DDL cleanup and type comparison equivalence.
- [ ] 1.4 Add compatibility-suite case covering Oracle create + OB compatibility behavior.
